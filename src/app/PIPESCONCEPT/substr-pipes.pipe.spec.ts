import { SubstrPipesPipe } from './substr-pipes.pipe';

describe('SubstrPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new SubstrPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
